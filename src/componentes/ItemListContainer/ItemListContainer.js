import * as React from 'react';
import {h1} from 'react-bootstrap';
const ItemListContainer = ({titulo}) => {
    return (
  <div>
    <h1 color='red'>{titulo}</h1>
  </div>
    );
  }
  export default ItemListContainer